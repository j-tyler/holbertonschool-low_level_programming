#include <stdio.h>

/**
 * main - prints the filename of this program
 * @argc: argument count
 * @argv: argument array
 *
 * Return: Always 0
 */

int main(int __attribute__((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
